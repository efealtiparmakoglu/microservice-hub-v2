apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: microservice-hub-v2-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: microservice-hub-v2.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: microservice-hub-v2-service
            port:
              number: 80
